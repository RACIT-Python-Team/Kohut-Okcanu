Asked the user for an integer and printed it with an exclamation mark.

Converted the entered integer to binary and hexadecimal formats.

Solved the equation 4x + a*(x-1) = 4, considering cases with no solution.

Calculated the expression (((a+2)**3)/4)-5 for a float number.

Converted a float to an integer.

Asked for the user’s name and printed a greeting.

Used round, max, min to find rounded numbers and min/max values.

Found the maximum and minimum of three input numbers.

Calculated the product of two integers (set of numbers).

Implemented a basic calculator for +, -, *, /.

Solved simple one-variable equations (7x - a = 0, 45/(3*a)).

Performed string operations: repeated text a given number of times, combined text and numbers.
